---
date: 2026-04-25
session: 22
scope: internal/gateway/{runtime_run_execute,consensus,persistence,runtime_persist,runtime_archive}.go (~1072줄) — 실행 디스패치 + consensus + persistence (2/3)
next: internal/gateway/{runtime_channels,runtime_executors,runtime_nodes,runtime_reports,resolve_override,execute_context}.go (s23, ~870줄) — channels + reports + 나머지. gateway 패키지 종료
findings: [RF-052, RF-053, RF-054, Q-014, Q-015, ID-005 인벤토리 보강]
---

# Session 22 — `internal/gateway/` 실행 디스패치 + consensus + persistence (2/3)

## 다룬 파일

- `runtime_run_execute.go` (355) — `executeRun` 본체 + `finalizeRunLocked` 200줄 + diagnostic + `appendRunSummaryToMain`
- `consensus.go` (258) — `runConsensus` (7-layer budget guard) + `executeVariantPrompt` + `aggregateConsensus`
- `persistence.go` (145) — `snapshotStore` + atomic write + `trimRuns`/`trimChannels`
- `runtime_persist.go` (206) — `restoreSnapshotOnStartup` + `persistSnapshot` (2-attempt retry)
- `runtime_archive.go` (108) — JSONL archive + rotation + retention

총 ~1072줄.

## 한눈에

### 실행 디스패치 (runtime_run_execute)

`executeRun` flow (s21 의 framework + s22 의 본체):
```
acquireExecutionSlot (executionSem) →
startRunExecution (Running 상태로) →
executeRunPrompt (consensus 분기 또는 normal LLM call) →
finalizeRunLocked (status + diagnostic + events + summary)
```

**`appendRunSummaryToMain`** — worker session (`Hidden=true`, `Kind=worker`) 의 결과를 parent main session 에 `[RUN SUMMARY]` system message 로 append. 사용자가 main session 에서 worker run 결과 보임.

### Consensus (consensus.go)

같은 prompt 를 여러 provider variant 로 parallel 실행 + aggregate:

**7-layer budget guard** ⭐:
1. `GatewayConsensusEnabled` flag
2. fanout cap
3. token pre-check (estimatedTokens vs budget)
4. USD pre-check (estimatedUSD vs budget)
5. timeout (default 120s)
6. runtime token cap (atomic counter, 초과 시 ctx cancel)
7. 2 semaphore (consensusRuns + consensusPool)

**동시성**:
- sync.WaitGroup (모든 variant 대기)
- sync.Mutex (records 보호)
- atomic.Int64 (token sum, lock-free)

**Strategy: synthesize 만 구현** [RF-052]:
- "synthesize": 모든 successes 답을 LLM 에 보내 best 합성
- "vote": 미구현 (schema 에는 노출)

### Persistence (persistence + runtime_persist + runtime_archive)

**스토어 구조**:
- `runs.json` (모든 runs 직렬화)
- `channels.json` (workspace:channel → messages)
- `gateway-YYYYMMDD.jsonl` archive (JSONL append, rotation)

**atomic write** (`writeJSONAtomic`): tmp create → write → sync → close → rename.

**Restore on startup**:
- in-flight runs (Accepted/Running) → `RunStatusCanceled` + `"canceled by restart recovery"`
- `parseIDSequence` — restored data 의 가장 큰 sequence 로 atomic counter 갱신 (id 충돌 방지)

**Persist trigger**: `persistSnapshot` 이 매 state 변경 후 호출. **2-attempt retry** (Q-014):
```go
for attempt := 0; attempt < 2; attempt++ {
    runs, channels, snapshotVersion := r.snapshotForPersistence()
    // write
    if currentVersion == snapshotVersion || attempt == 1 { return }
}
```

**Archive rotation**:
- 파일명: `gateway-YYYYMMDD.jsonl`, `gateway-YYYYMMDD-1.jsonl`, ...
- 10MB 초과 시 다음 인덱스
- `cleanupGatewayArchiveFiles`: retention days 후 삭제

## 좋은 패턴 ⭐⭐

### 1. consensus 의 7-layer budget guard (s22 신규 모범 패턴)

`runConsensus` 의 사전+런타임 가드 7단계 = **모범 보안 가드 9번째**:

s12-s21 의 8 가드 + s22 의 consensus budget guard:
1. RegistryScope (s12)
2. PathPolicy (s13)
3. web_fetch SSRF (s14)
4. subagent triple guard (s16)
5. flow validation (s17)
6. NewRouter 7-validation (s18)
7. OpenAI Codex OAuth 자동 갱신 (s20)
8. CommandExecutor env injection 가드 (s21)
9. **consensus 7-layer budget guard** (s22)

→ 사전 검증 (token + USD pre-check) + 런타임 abort (atomic counter ctx cancel) + 자원 cap (semaphore × 2). RF-035 (process timeout 30초) 보다 정교.

### 2. consensus 동시성 표준 패턴

```go
var (
    records   = make([]ConsensusVariantRecord, len(resolved))
    tokenSum  atomic.Int64
    mu        sync.Mutex
    wg        sync.WaitGroup
)
for idx := range resolved {
    idx := idx
    wg.Add(1)
    go func() {
        defer wg.Done()
        // semaphore acquire/release
        // execute variant
        // mu.Lock + records[idx] = ... + mu.Unlock
        // tokenSum.Add → budget 초과 시 cancel()
    }()
}
wg.Wait()
```

→ Go 동시성 4 도구 (WG + Mutex + atomic + ctx) 결합. 표준 패턴. RF-021 (rebuildArtifacts O(N)) 같은 직렬화 안티패턴과 대조.

### 3. persist snapshot 의 optimistic concurrency

snapshot 읽고 write 사이 state 변경 시 재시도. 2 attempt cap. **Q-014 의 효용 평가** 권장.

### 4. atomic file write

tmp + sync + rename. 표준 atomic write. CLAUDE.md *"persistence in workspace/_shared/gateway/"* 약속과 정렬. 모든 persist 작업이 이 패턴 사용.

### 5. parseIDSequence — id collision 방지

```go
if seq := parseIDSequence(run.ID, "run_"); seq > 0 {
    current := r.runSeq.Load()
    if seq > current {
        r.runSeq.Store(seq)
    }
}
```

→ restored data 의 가장 큰 sequence 로 atomic counter 갱신. 다음 spawn 시 id 충돌 방지. **세심한 패턴**.

### 6. archive rotation + retention

`gateway-YYYYMMDD.jsonl` (10MB cap) → `gateway-YYYYMMDD-1.jsonl` 등 자동 rotation. 30일 retention default. 안전한 디스크 사용.

### 7. policy 진단 정보 추출

`finalizeRunLocked` 가 err 메시지에서 policy block 정보 (tool/rule/group/source) 추출:
```go
if blocked, ok := blockedToolErrorFromReason(...); ok {
    state.run.PolicyBlockedTool = blocked.Tool
    state.run.PolicyBlockedRule = blocked.Rule
    ...
}
```

→ 사용자 facing 진단을 풍부하게. tool/policy.go (s12) 의 `BlockedToolError.Error()` 와 reverse parse 패턴.

### 8. appendRunSummaryToMain — worker→main session 정보 흐름

worker session (Hidden=true, Kind=worker) 의 결과를 parent main session 에 자동 forward. 사용자가 main session 에서 보임. **gateway 의 사용자 facing 핵심 가치**.

## 새 발견 (RF급)

### RF-052 — consensus strategy "vote" 미구현 — schema/구현 mismatch

`tool/tool_subagents.go:27` schema 에 `enum: ["synthesize", "vote"]` 노출. `aggregateConsensus` 는 synthesize 만 구현 + 다른 strategy 시 silent runtime error. **YAGNI 위반 stub** 카테고리.

### RF-053 — `finalizeRunLocked` 200줄 + 6 책임

성공/실패 분기 분리 가치. RF-019 카테고리 (가독성 개선).

### RF-054 — 동일 event type 2번 publish

`run_failed` / `run_finished` 각각 finalize 안에서 2번 publish. subscriber duplicate 처리 부담. 한 번에 통합 가능.

## 새 발견 (Q급)

### Q-014 — `persistSnapshot` 의 2-attempt retry 효용

retry 빈도가 0 가까우면 single-attempt 로 단순화 가능. 운영 데이터 측정 후 결정.

### Q-015 — consensus 모드의 실제 활성 빈도

258줄 + 7-layer guard + 4 타입 (~400줄+ 표면). LLM 의 일반 chat 에서 mode="consensus" 호출 빈도 의문. usage tracker 데이터 후 외부화/유지 결정.

## ID-005 인벤토리 보강 (s22)

### 패키지 내부 식별자 (s22 추가)

`runConsensus` / `executeVariantPrompt` / `aggregateConsensus` / `sanitizeConsensusVariants` / `validateConsensusAlias` / `estimateCostUSD` / `estimateTextTokens` / `snapshotStore` / `runsSnapshot` / `channelsSnapshot` / `writeJSONAtomic` / `readJSONFile` / `trimRuns` / `trimChannels` / `persistenceEnabled` / `restoreSnapshotOnStartup` / `persistSnapshot` / `snapshotForPersistence` / `parseIDSequence` / `splitWorkspaceChannelKey` / `persistArchiveSnapshot` / `selectGatewayArchiveFile` / `cleanupGatewayArchiveFiles` / `finalizeRunLocked` / `acquireExecutionSlot` / `releaseExecutionSlot` / `appendSessionMessage` / `appendRunSummaryToMain` / `buildRunSummaryMessage` / `trimGatewaySummary` / `blockedTool*FromReason` 함수들

→ 약 35 추가 식별자 (s21 약 35 + s22 약 35 = ~70 식별자). 모두 패키지 내부. **타입명 유지 OK**.

### 코드 본문 내 "gateway" 문자열 (s22 신규)

| 위치 | 문자열 | 변경 필요? |
|------|--------|-----------|
| `consensus.go:38` | `"consensus is disabled (gateway_consensus_enabled=false)"` | ⚠️ config field 이름 변경 시 |
| `consensus.go:49` | `"gateway_consensus_max_fanout=%d"` | 동일 |
| `runtime_archive.go:59, 61` | archive 파일명 prefix `"gateway-"` | ⚠️ **읽기 호환성 필요** |
| `runtime_archive.go:95` | archive cleanup prefix `"gateway-"` | 동일 |
| `runtime_archive.go:`함수명 | `cleanupGatewayArchiveFiles`, `selectGatewayArchiveFile`, `persistArchiveSnapshot` | 패키지 내부 변경 |

### **새 마이그레이션 항목 (s22 신규)**: archive 파일 prefix

기존 사용자 워크스페이스의 `_shared/gateway/archive/gateway-YYYYMMDD.jsonl` 파일들이 옛 prefix. ID-005 변경 시:

옵션:
- **(a) prefix 동시 호환** — 새 prefix 로 write + 옛 prefix 도 read (cleanup). 마이그레이션 부담 0
- **(b) startup 자동 rename** — `gateway-*.jsonl` → `agentruntime-*.jsonl`
- **(c) prefix 유지** — archive 는 사용자 facing 식별자 아니므로 옛 prefix 유지 가능

→ **(c) 권장** — archive 파일은 운영 도구 (분석/모니터링)가 보는 표면이라 명시적 hard cut 비용 큼. 함수명만 변경 (cleanupGatewayArchiveFiles → cleanupArchiveFiles 등).

→ ID-005 본문 Phase 6 (잔재 정리) 에 archive prefix 정책 추가.

### Phase 추정 검증 (s21+s22 데이터)

ID-005 본문의 6 phase 추정:
- ✅ **Phase 1-2** 자동화 비율 70-80% — s22 데이터로 확인 (식별자 ~70개, 본문 문자열 ~10건)
- ✅ **Phase 3** config 35+ field — gateway_* prefix 변경 + 호환성 결정
- ✅ **Phase 4** HTTP URL — s22 영향 없음
- ✅ **Phase 5** 빌트인 툴 — s22 영향 없음
- 🆕 **Phase 6** 잔재 정리 — archive 파일 prefix 정책 추가 (s22 신규)

→ 추정 정확도 유지. **archive prefix 결정** 만 ID-005 본문에 보강 필요.

## RF-042 (strict mode) 의 consensus 적용 가능성

`aggregateConsensus` 가 LLM 한 번 호출 (synthesize):
```go
b.WriteString("Synthesize the best final answer ...")
```

→ 자유 텍스트 응답. strict structured output 적용 후보. 다만:
- aggregator 가 *"마지막 답"* 만 필요 → 자유 텍스트 더 자연스러움
- subagents_plan (s17) 이 RF-042 의 더 명확한 후보
- consensus 빈도 의문 (Q-015) → 우선순위 낮음

→ **RF-042 의 consensus 적용은 후순위**. ID-004 Phase 1 (OpenAI ResponseFormat) 후 검토.

## 새 관점 ("필요한가/유용한가") 평가

| 영역 | 평가 |
|------|------|
| consensus 알고리즘 + 7-layer guard | ⚠️ Q-015 — 활성 빈도 평가 후 결정 |
| persist snapshot + atomic write + restore | ✅ 필수 (gateway 의 핵심 resilience) |
| archive (rotation + retention) | ⚠️ runs/channels persist 와 별개. 사용 빈도? 콘솔 UI 또는 외부 도구만 활용? |
| `aggregateConsensus` synthesize 만 구현 | ⚠️ RF-052 — vote schema 정리 |
| `appendRunSummaryToMain` (worker→main) | ✅ 사용자 facing 가치 큼 |
| `finalizeRunLocked` 200줄 | ⚠️ 가독성 [RF-053] |
| `publishRunEvent` 중복 | ⚠️ [RF-054] |

→ **consensus + archive** 빈도 평가 후 외부화/단순화 결정. 나머지 필수.

## CLAUDE.md 정렬

| 약속 | 실제 | 평가 |
|------|------|------|
| *"runtime state machine"* | RunStatus 5 + transitions 명확 | ✅ |
| *"run persistence in workspace/_shared/gateway/"* | snapshotStore + atomic write | ✅ |
| *"max 4 subagents"* | GatewaySubagentsMaxThreads cap | ✅ |
| *"agent execution platform"* | 패키지 description 정확 | ✅ ID-005 변경 시 명명도 정렬 |
| *"Avoid backwards-compatibility hacks"* | RF-052 (vote stub) | ⚠️ |
| *"Don't add features beyond what the task requires"* | RF-052 + Q-015 (consensus over-engineering 의문) | ⚠️ |

## 핵심 인사이트

### gateway 패키지 2/3 종합

**Resilience 의 3 axis** (s22):
1. **Persistence**: snapshot + atomic write + 2-attempt retry
2. **Restore**: in-flight cancel + id sequence 갱신
3. **Archive**: rotation + retention

→ gateway 가 *"crash-resistant"* 인프라. CLAUDE.md *"crash-resistant idempotence"* (pulse/reflection 기록) 의 본거지.

### Consensus 의 정교함 vs 활용 빈도

7-layer budget guard 는 매우 신중한 설계 — over-engineering 가능. Q-015 의 정량 데이터로 결정:
- 빈도 높으면 RF-052 (vote 구현) + RF-042 적용
- 빈도 낮으면 외부화 또는 deprecate

### finalizeRunLocked 의 짐

200줄 + 6 책임 + duplicate publish — gateway 의 가장 큰 함수. RF-053/054 가 가독성 + 효율 양쪽 개선. 작은 RF 지만 ROI 큼.

### ID-005 진척

s21+s22 후 인벤토리:
- 패키지 내부 식별자: ~70개 (변경 0)
- 코드 본문 "gateway" 문자열: ~10+ 건 (자동화 + 수동)
- 새 발견: **archive 파일 prefix** 호환성 결정 (Phase 6 보강)

→ s23 (channels) 후 인벤토리 완성. Phase 0 사전 결정 입력 충분.

## 다음 세션 진입점 (s23, gateway 마지막)

`internal/gateway/` 의 남은 파일들:

- `runtime_channels.go` (150) — channel messaging (send/read/inbound)
- `runtime_executors.go` (214) — executor 등록 + agents 정보
- `runtime_nodes.go` (63) — gateway nodes (extension)
- `runtime_reports.go` (224) — report (summary/runs/channels)
- `resolve_override.go` (88) — provider override resolution
- `execute_context.go` (28) — context helpers

총 ~767줄. gateway 패키지 종료 → s24 부터 session/config/cron/frontend.
