---
date: 2026-04-25
session: 25
scope: internal/config/{types,load,schema,llm_providers_field,llm_resolve,gateway_task_override_field,mcp,legacy,merge,env}.go (~1583줄) — config 1/2 (schema + types + LLM resolve)
next: internal/config 2/2 (s26) — defaults + yaml + parse_helpers + config_input_fields (~1700줄) → s27 cron/frontend
findings: [RF-062, RF-063]
---

# Session 25 — `internal/config/` schema + types + LLM resolve (1/2)

## 다룬 파일

- `types.go` (328) — Config + 14 sub-config struct + LLM provider pool/tier/role
- `load.go` (97) — 4-step Load + PatchYAML + ResolveConfigPath
- `schema.go` (566) — 모든 필드의 FieldMeta (UI 자동 렌더링)
- `llm_resolve.go` (134) — ResolveLLMTier 7-validation + ResolveAllLLMTiers
- `llm_providers_field.go` (184) — 3 JSON parser (provider/tier/role) + env expansion
- `gateway_task_override_field.go` (67) — JSON parser
- `mcp.go` (104) — MCPServer normalization + transport/auth mode
- `legacy.go` (93) — removed key migration warning (doctor 명령에서 사용)
- `merge.go` (5) + `env.go` (5) — config_input_fields dispatch

총 ~1583줄.

## 한눈에

### Config 14 sub-struct (~350 field)

```
RuntimeConfig    — mode/workspace/log
APIConfig        — auth tokens (3 tier) + inflight caps
LLMConfig        — provider pool + tiers + default + role defaults
MemoryConfig     — semantic embed
UsageConfig      — daily/weekly/monthly USD
AutomationConfig — pulse + reflection + cron + agent
AssistantConfig  — voice
CompactionConfig — token thresholds
ToolConfig       — 17+ tool flags
VaultConfig      — secrets
BrowserConfig
GatewayConfig    — 35+ field (ID-005 Phase 3 핵심)
ChannelConfig    — local/webhook/telegram
ExtensionConfig  — skills + plugins + MCP
```

embed promotion → cfg.GatewayConsensusEnabled / cfg.PulseEnabled / etc 직접 접근. RF-062.

### LLM 3-layer (CLAUDE.md *"Provider pool"* 의 본거지)

```
Role (chat_main/pulse_decider/...)
   ↓ LLMRoleDefaults map (config-driven)
Tier (heavy/standard/light)
   ↓ LLMTierBinding (provider alias + model + reasoning)
Provider Alias (codex/claude/gemini/...)
   ↓ LLMProviderSettings (kind + auth + base_url + api_key)
실제 LLM 호출
```

→ 같은 provider 가 여러 tier 에 매핑 가능 (codex 가 standard 에는 4-mini, heavy 에는 5-codex). **모범 패턴**.

### ResolveLLMTier 7-validation (보안 가드 11번째)

`llm_resolve.go:55-108`:
1. cfg != nil
2. tier != ""
3. cfg.LLMTiers[tier] 존재
4. binding.Provider != ""
5. cfg.LLMProviders[alias] 존재
6. provider.Kind != ""
7. binding.Model != ""

→ **모두 fail loud** — silent fallback 없음. CLAUDE.md *"single resolution path, loud errors"* 정확. s18 의 NewRouter 7-validation 의 사전 단계 → 둘이 합쳐 **14-validation**.

### Load 4-step

```
Default() → loadYAML(path) → applyEnv → applyDefaults
```

→ 명시적 우선순위. CLAUDE.md *"YAML → env var override → defaults"* 정렬.

### Schema FieldMeta UI 자동 렌더링

`Schema()` 가 모든 필드 메타 (key/section/type/label/description/sensitive/options) export. 콘솔 UI 가 자동 폼 렌더링. 추상화 가치 큼.

## 좋은 패턴 ⭐⭐ (모범 패턴 다수)

### 1. ResolveLLMTier 7-validation = 보안 가드 11번째

s12-s23 의 10 가드 + s25 의 ResolveLLMTier:
1. RegistryScope (s12)
2. PathPolicy (s13)
3. web_fetch SSRF (s14)
4. subagent triple guard (s16)
5. flow validation (s17)
6. NewRouter 7-validation (s18)
7. OpenAI Codex OAuth 자동 갱신 (s20)
8. CommandExecutor env injection 가드 (s21)
9. consensus 7-layer budget guard (s22)
10. ResolveOverride task override 7-layer 가드 (s23)
11. **ResolveLLMTier 7-validation** (s25)

→ 11 가드 누적. NewRouter 7-validation (s18) 과 합쳐 **14-validation 체인**. 코드베이스의 *"silent skew 방지"* 의식 정점.

### 2. LLM provider pool 3-layer 추상화

`LLMProviderSettings` (where + how to authenticate) + `LLMTierBinding` (provider alias + model + reasoning) + `LLMRoleDefaults` (role → tier).

**핵심 가치**: 같은 provider 가 여러 tier 에 매핑. e.g.
```yaml
llm_providers:
  anthropic_main: { kind: anthropic, api_key: ${KEY1} }
llm_tiers:
  heavy:    { provider: anthropic_main, model: claude-opus-4-7 }
  standard: { provider: anthropic_main, model: claude-sonnet-4-6 }  # 같은 provider
  light:    { provider: anthropic_main, model: claude-haiku-4-5 }   # 같은 provider
```

→ credentials 1번만 정의. CLAUDE.md *"One provider alias can serve multiple tiers"* 정렬.

### 3. JSON env override + nested expansion

```go
out[alias] = LLMProviderSettings{
    Kind:    os.ExpandEnv(strings.TrimSpace(p.Kind)),
    APIKey:  os.ExpandEnv(strings.TrimSpace(p.APIKey)),  // ${ANTHROPIC_API_KEY} 정상
    ...
}
```

→ TARS_LLM_PROVIDERS_JSON 환경변수 안에 `${ANTHROPIC_API_KEY}` 박혀 있어도 자동 expand. **shared YAML loader 가 top-level scalar 만 expand 하는 한계** 우회. CLAUDE.md *"single-JSON env override"* 약속의 본거지.

### 4. Legacy migration warning (사용자 친화)

removed key 발견 시 명시적 안내:
- 9 flat LLM key (`llm_provider`/`llm_auth_mode`/`llm_model` 등) → `llm_providers` + `llm_tiers` 안내
- 3 heartbeat key (`heartbeat_*`) → `pulse_*` 안내
- 3 legacy prefix (`llm_tier_heavy_*`/`llm_role_*` 등) → 새 nested 형식 안내

→ `tars doctor` 명령에서 사용 (확인 완료). 사용자에게 *"무엇으로 마이그레이션해야 하는지"* 명시. CLAUDE.md *"Avoid backwards-compatibility hacks ... // removed comments"* 와 부분 충돌 — 그러나 **사용자 마이그레이션 가이드** 는 정당.

### 5. Schema FieldMeta UI 자동 렌더링

```go
type FieldMeta struct {
    Key, Path, Section, Type, Label, Description string
    Sensitive bool
    Options []string
}
```

→ 콘솔 UI 가 폼 자동 생성 + sensitive 마스킹 + select options. **추상화 가치 큼**.

### 6. MCPServer normalization 일관성

transport (stdio/streamable_http/sse/websocket) + auth mode (none/bearer/oauth) + 별칭 처리 (`http`/`streamable-http` → `streamable_http`, `ws` → `websocket`). 정합성.

### 7. PatchYAML — UI 부분 업데이트

콘솔 UI 가 일부 필드 변경 시 사용. key normalization + delete representations + set value. 전체 YAML 재작성 안 함.

## 새 발견 (RF급)

### RF-062 — Config 의 14 embed = ~350 field flat access

embed promotion → caller 가 도메인 명시 없이 직접 접근. autocomplete 만 의존. namespaced (`cfg.Gateway.ConsensusEnabled`) 가 더 명시적이지만 마이그레이션 비용 큼. **(c) 현 상태 유지 권장** (Go ecosystem 표준 패턴).

### RF-063 — ServiceTier 2-level docstring 명시화

LLMProviderSettings.ServiceTier (provider default) + LLMTierBinding.ServiceTier (tier override). 2-level 의도. caller 헷갈림 방지 docstring.

## 새 관점 ("필요한가/유용한가")

| 영역 | 평가 |
|------|------|
| Config 14 sub-struct | ✅ 필수 (도메인 분리) |
| **LLM 3-layer** | ⭐⭐ 모범 (CLAUDE.md 약속의 본거지) |
| **ResolveLLMTier 7-validation** | ⭐⭐ 보안 가드 11번째 |
| **JSON env override + nested expansion** | ⭐⭐ 모범 |
| Schema FieldMeta UI 자동 렌더링 | ✅ 가치 |
| Legacy migration warning | ✅ 사용자 친화 (doctor 에서 활성) |
| MCPServer normalization | ✅ 정합성 |

→ 모든 영역 정당. config 패키지가 **CLAUDE.md 약속의 본거지** + 보안 가드 11번째.

## ID-005 Phase 3 인풋 (s25)

config 측 마이그레이션 항목:
- **GatewayConfig 35+ field** rename (`gateway_*` → `agent_runtime_*` 또는 `runtime_*`)
- **types.go** field name (자동화 sed)
- **schema.go** FieldMeta key + section (자동화)
- **legacy.go** deprecation warning 에 새 매핑 추가 (수동, 옛 사용자 안내)
- **yaml_paths.go** preferredYAMLPathForKey 매핑 (s26)
- **defaults.go** Default() (s26, 변경 0)
- **defaults_apply.go** Gateway 관련 default 적용 (s26, 자동화)
- **config_input_fields.go** Gateway 관련 yamlKey + envKeys (s26, 자동화)

→ **자동화 비율 80%+**. ID-005 본문의 Phase 3 추정 (2일) 정확.

## CLAUDE.md 정렬

| 약속 | 실제 | 평가 |
|------|------|------|
| *"YAML → env var override → defaults"* | load.go 4-step | ✅ |
| *"60+ config fields"* | Config 14 embed = ~350 field | ✅ (60+ 보다 더 큼) |
| *"single resolution path, loud errors"* | ResolveLLMTier 7-validation | ⭐⭐ |
| *"Provider alias can serve multiple tiers"* | LLMProviderSettings + LLMTierBinding | ⭐⭐ |
| *"single-JSON env override"* | parseLLMProvidersJSON + os.ExpandEnv | ⭐⭐ |
| *"config package does not validate Kind"* | provider.Kind 만 빈 검사, 닫힌 list 미검증 (llm.NewProvider 책임) | ✅ |

## 핵심 인사이트

### config 패키지의 정체성

CLAUDE.md *"single resolution path, loud errors"* 의 **본거지**:
- ResolveLLMTier 7-validation (보안 가드 11번째)
- NewRouter 7-validation (s18) 의 사전 단계
- 둘이 합쳐 14-validation 체인 — **wiring-time loud failure**

→ TARS 의 *"잘못된 config 가 startup 에서 즉시 fail"* 약속의 본거지.

### LLM provider pool = ID-004 의 호환 인풋

ID-004 (OpenAI/Gemini capability 격차 해소) 의 작업이 이 config 위에서 진행:
- 새 provider 추가 시 LLMProviderSettings 재사용
- ChatOptions 확장 (ResponseFormat 등) 시 LLMTierBinding 에 추가
- 기존 *"3-layer"* 추상화 유지

→ ID-004 와 config 패키지의 **상호 의존성**.

### 보안 가드 11종 누적 (s25 종료 시점)

1-10. 이전 누적
11. **ResolveLLMTier 7-validation** (s25)

→ 모두 wiring/input-time 정합성 검증. 코드베이스 11종 누적 = **명시적 안전성 의식**.

## 다음 세션 진입점 (s26)

`internal/config/` 2/2:
- `defaults.go` (234) + `defaults_apply.go` (449) — 기본값 + 적용 로직
- `yaml.go` (185) — YAML loader
- `yaml_paths.go` (317) — yaml path 매핑
- `parse_helpers.go` (188) — int/float/bool/duration parser
- `config_input_fields.go` (355) — 통합 dispatch (apply/merge)

총 ~1728줄. **ID-005 Phase 3 의 마지막 인풋 검증** + config 패키지 종료.

검토 진척 50 / ~50. **2 세션 더면 종료** → `overview.md` 작성.
