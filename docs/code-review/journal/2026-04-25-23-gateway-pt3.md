---
date: 2026-04-25
session: 23
scope: internal/gateway/{runtime_channels,runtime_executors,runtime_nodes,runtime_reports,resolve_override,execute_context}.go (~767줄) — channels + executors + nodes + reports + override (3/3). **gateway 패키지 종료**
next: internal/session 또는 internal/config (s24)
findings: [RF-055, RF-056, RF-057, Q-016, ID-005 인벤토리 완성]
---

# Session 23 — `internal/gateway/` channels + executors + reports + override (3/3, gateway 종료)

## 다룬 파일

- `runtime_channels.go` (150) — channel 메시징 (Send/Read/ThreadReply + Inbound + Outbound)
- `runtime_executors.go` (214) — executor 등록 + agent 정보 + hot reload
- `runtime_nodes.go` (63) — 3 hardcoded node (echo / clock.now / sessions.latest)
- `runtime_reports.go` (224) — Status + ReportsSummary + ReportsRuns + ReportsChannels + Reload + Restart
- `resolve_override.go` (88) — provider override resolution + 다층 가드
- `execute_context.go` (28) — context.Context 메타데이터 helpers

총 ~767줄.

## 한눈에

### Channels (runtime_channels)

6 public method (3 send/read + 3 inbound/outbound):
- `MessageSend` / `MessageRead` / `ThreadReply` — local channel
- `InboundWebhook` — webhook 채널 inbound
- `InboundTelegram` / `OutboundTelegram` — telegram

모두 `*ByWorkspace` 변종 + default workspace alias. 내부 헬퍼 `appendChannelMessage` 가 공통 path:
- direction (inbound/outbound) + source (local/webhook/telegram)
- workspace 격리 (`workspaceChannelKey` = `<workspace>:<channel>`)
- max messages per channel cap
- atomic ID 생성 + snapshot persist

### Executors (runtime_executors)

- `initExecutors` — 생성자에서 호출
- `SetExecutors` — runtime hot reload (콘솔 UI *"Reload Agents"* backbone)
- **fallback default executor** — `RunPrompt` callback 있으면 "default" agent 자동 등록
- `resolveExecutor` — agent name → executor (3-level fallback: requested → "default" → first sorted)
- `Agents()` — 콘솔 UI 용 export (큰 map 16+ field)
- `LookupAgent` / `SubagentLimits`

### Nodes (runtime_nodes)

3 hardcoded:
- `echo` — args 그대로 반환 (dummy)
- `clock.now` — time.Now().UTC()
- `sessions.latest` — 가장 최근 세션 메타

→ **각 node 가 이미 다른 곳에서 더 자연스럽게 노출** (RF-055).

### Reports (runtime_reports)

- `Status()` — gateway 전체 status (agents/channels/persistence/restore/nodes 모두 포함)
- `ReportsSummary*` — runs by status + channels by source 카운트
- `ReportsRuns*` / `ReportsChannels*` — 상세 리스트 (archive flag 가드 — RF-057)
- `Reload` (version++) / `Restart` (모든 in-flight cancel + version++)

### ResolveOverride (resolve_override)

**보안 가드 10번째** ⭐ — task override 다층 가드:

```
1. cfg.GatewayTaskOverride.Enabled
2. alias != "" required
3. AllowedAliases 화이트리스트
4. cfg.LLMProviders[alias] 등록 검증
5. model 결정 (override → resolved fallback)
6. AllowedModels 화이트리스트
7. api-key 모드 시 APIKey 필수 검증
```

→ caller (subagents_run/orchestrate consensus variants) 가 사용자 정의 LLM 호출 시 신중한 가드.

### ExecuteContext (execute_context)

context.Context 에 `PromptExecutionContext` (override + source + metadata) 부착. executor → LLM run 사이 메타데이터 전달.

## 좋은 패턴 ⭐

### 1. ResolveOverride 의 7-layer 가드 (보안 가드 10번째)

s12-s22 의 9 가드 + s23 의 ResolveOverride:

1. RegistryScope (s12)
2. PathPolicy (s13)
3. web_fetch SSRF (s14)
4. subagent triple guard (s16)
5. flow validation (s17)
6. NewRouter 7-validation (s18)
7. OpenAI Codex OAuth 자동 갱신 (s20)
8. CommandExecutor env injection 가드 (s21)
9. consensus 7-layer budget guard (s22)
10. **ResolveOverride task override 가드** (s23)

→ **10 가드 모두 wiring/input-time 정합성 검증**. CLAUDE.md *"single resolution path, loud errors"* 정렬.

### 2. Channel 메시징의 일관된 path

6 public method 모두 `appendChannelMessage` 헬퍼로 위임. direction + source + workspace 격리 + cap + persist 일관 적용. **단일 path 정합성**.

### 3. Executor hot reload + 3-level default fallback

```go
SetExecutors(executors, defaultAgent) →
  applyExecutorsLocked →
    1. requested defaultAgent 있으면 사용
    2. "default" 등록 있으면 사용
    3. 첫 sorted executor 사용
```

→ **resilient fallback**. config 부족해도 동작.

### 4. Workspace 격리 (channel level)

`workspaceChannelKey(workspaceID, channelID)` = `<workspace>:<channel>` — 다른 workspace 의 같은 channelID 가 충돌 안 함. 다중 사용자 시나리오 대비.

### 5. Restart 의 in-flight cancel

`Restart()` 가 모든 Accepted/Running run 을 *"canceled by gateway restart"* 로 변환. version++ + persist. 명확한 graceful restart.

## 새 발견 (RF급)

### RF-055 — runtime_nodes.go 의 3 hardcoded node 가 dead/demo

각 node 이미 다른 곳에서 더 자연스럽게 노출:
- `echo` — dummy
- `clock.now` — system prompt 가 이미 주입 (s11)
- `sessions.latest` — sessions_list 가 더 자연스러움

RF-040 (message/nodes/gateway 통합/외부화) 와 결합 정리 권장.

### RF-056 — Inbound/OutboundTelegram channelID fallback 불일치

```go
// Inbound: botID → "telegram"
// Outbound: chatID → botID → "telegram"
```

→ 의도 있을 가능성 (inbound = bot-level 통합, outbound = chat-level 분리) 다만 **코멘트 없음**. 헬퍼 추출 + 의도 명시.

### RF-057 — `ReportsRuns`/`ReportsChannels` archive flag 의미 모호

`GatewayArchiveEnabled` 가 두 의미 중첩:
- (1) 디스크 archive 활성
- (2) in-memory report endpoint 권한

→ 분리 또는 명시화. ID-005 (config 마이그레이션) 와 결합 가능.

## 새 발견 (Q급)

### Q-016 — runtime_nodes + NodesTool 사용 빈도

3 hardcoded node + cfg-flag optional NodesTool. 사용자 LLM 의 일반 워크플로 에서 빈도 의문. (a) 거의 0 — RF-055 적용. (b) demo. (c) 외부 헬스체크.

## ID-005 인벤토리 완성 (s23)

### 패키지 내부 식별자 (s23 추가)

`MessageSend`/`MessageRead`/`ThreadReply` (+ ByWorkspace) / `InboundWebhook`/`InboundTelegram`/`OutboundTelegram` (+ ByWorkspace) / `appendChannelMessage` / `workspaceChannelKey` / `initExecutors` / `SetExecutors` / `SetAgentsWatchEnabled` / `applyExecutorsLocked` / `markAgentsReloadLocked` / `registerExecutorLocked` / `executorNamesLocked` / `resolveExecutor` / `Agents` / `LookupAgent` / `SubagentLimits` / `gatewayAgentInfo` / `Nodes` / `NodeDescribe` / `NodeInvoke` / `defaultNodes` / `ResolveOverride` / `containsExactString` / `WithPromptExecution` / `PromptExecutionFromContext` / `PromptExecutionContext` / `Status` / `ReportsSummary` / `ReportsRuns` / `ReportsChannels` (+ ByWorkspace) / `Reload` / `Restart`

→ s23 ~30 추가. **누적 ~135 식별자**. 모두 패키지 컨텍스트로 명확. 타입명 유지 OK.

### 코드 본문 "gateway" 문자열 (s23 신규)

| 위치 | 문자열 | 변경 |
|------|--------|------|
| `runtime_reports.go:124, 147` | `"gateway archive report is disabled"` | config field 변경 시 |
| `resolve_override.go:36` | `"gateway_task_override is disabled"` | 동일 |
| `resolve_override.go:43, 54` | `"gateway_task_override.allowed_*"` | 동일 |
| `runtime_reports.go:211` | `"canceled by gateway restart"` | 메시지 정리 |

→ 약 5 건. 자동화 + 수동 검토 혼합.

### `gatewayAgentInfo` 함수명

executor.go 의 `gatewayAgentInfo(executor) AgentInfo` — 패키지 내부 헬퍼. 함수명에 "gateway" 들어가지만 의미상 단순 *"executor.Info() wrapper"*. 변경 검토: `agentInfoFromExecutor` 또는 `executorInfo`.

### gateway 3세션 인벤토리 종합

| 영역 | 개수 | 비고 |
|------|------|------|
| 패키지 내부 식별자 | **~135** | 변경 0 (패키지명 변경만) |
| 코드 본문 "gateway" 문자열 | **~10+** | 자동 + 수동 |
| Public Runtime API method | **~50** | 시그니처 그대로 |
| Config field (gateway_*) | **35+** | 호환 alias 또는 hard cut |
| HTTP URL (/v1/gateway/*) | **7+** | hard cut + 콘솔 UI 동시 |
| Persistence dir | **1** | startup 자동 마이그레이션 |
| Archive prefix | "gateway-" | (c) 유지 권장 |
| 함수 내 "gateway" 단어 | **~5** | gatewayAgentInfo, persistArchiveSnapshot, etc. |

### Phase 추정 최종 검증 ✅

ID-005 본문의 6 phase 추정:
- ✅ Phase 1 (1-2일): 패키지 import + 타입 사용
- ✅ Phase 2 (2-3일): 변수명 + 파일명 + handler
- ✅ Phase 3 (2일): config 35+ field
- ✅ Phase 4 (1-2일): HTTP URL + 콘솔 UI
- ✅ Phase 5 (1일): 빌트인 툴 + 콘솔 UI
- ✅ Phase 6 (1일): 잔재 정리 + archive prefix 정책

**총 ~7-10일 추정 정확** ✅. 자동화 비율 **70-80%**.

### Phase 0 사전 결정 입력 (s23 종료 시점 정리)

ID-005 진행 전 사용자 confirm 필요:
1. **이름** — `agentruntime` (권장) vs `runtime`
2. **HTTP URL prefix** — `/v1/agentruntime/*` vs `/v1/runtime/*` vs alias 기간
3. **Config field 호환성** — hard cut vs 1 release alias
4. **Persistence 디렉토리** — auto-rename vs 수동
5. **Archive prefix** — (c) 옛 prefix 유지 권장
6. **PR 분할** — 5 PR 권장 (각 phase 별 e2e 검증)

→ 모든 결정 인풋 준비됨. ID-005 본문이 충분.

## 새 관점 ("필요한가/유용한가") 평가

| 영역 | 평가 |
|------|------|
| Channels (runtime_channels) | ✅ 필수 (telegram delivery 핵심) |
| Executors (runtime_executors) | ✅ 필수 (agent 등록 + hot reload) |
| **Nodes (runtime_nodes)** | ⚠️ Q-016 — RF-055 적용 후 제거 후보 |
| Reports (runtime_reports) | ✅ 필수 (콘솔 UI status) |
| ResolveOverride | ✅ 필수 (task override 보안 가드) |
| ExecuteContext | ✅ 필수 (executor 메타 전달) |

→ Nodes 만 의문. 나머지 다 필수.

## CLAUDE.md 정렬

| 약속 | 실제 | 평가 |
|------|------|------|
| *"local + webhook + telegram channels"* | runtime_channels.go 정확 | ✅ |
| *"workspace 격리"* | workspaceChannelKey 모든 채널 | ✅ |
| *"hot reload of agents"* | SetExecutors + agentsReloadVersion | ✅ |
| *"task override allowed_aliases/models"* | ResolveOverride 7-layer guard | ✅ |
| *"Avoid backwards-compatibility hacks"* | RF-055 (dead nodes) + RF-057 (archive flag 모호) | ⚠️ |

## 핵심 인사이트 — gateway 패키지 3세션 종합 (s21-s23)

### 5 책임 정리

```
[Layer 1] Run lifecycle    — 5 RunStatus + accepted→running→completed/failed/canceled
[Layer 2] AgentExecutor    — Info() + Execute(). PromptExecutor (메인) + CommandExecutor (wiring 0)
[Layer 3] Channels         — local/webhook/telegram + workspace 격리
[Layer 4] Consensus        — 7-layer budget guard + multi-variant + synthesize aggregate
[Layer 5] Persistence      — atomic snapshot + restore + archive rotation/retention
```

### 보안 가드 10종 누적 (gateway 종료)

1. RegistryScope (s12) — wiring time
2. PathPolicy (s13) — read/write 분리 + symlink escape
3. web_fetch SSRF (s14) — scheme/IP/DNS/redirect
4. subagent triple guard (s16) — agent kind + tool blocklist + prefix
5. flow validation (s17) — depth/threads/dup/dep dag
6. NewRouter 7-validation (s18) — boot-time loud failure
7. OpenAI Codex OAuth 자동 갱신 (s20)
8. CommandExecutor env injection 가드 (s21)
9. consensus 7-layer budget guard (s22)
10. **ResolveOverride task override 가드** (s23)

→ **10 가드 모두 일관된 의식**. 다른 영역 보안 코드의 reference 모델.

### gateway 의 dependency hub 역할 정량

`Runtime` 인스턴스가 캡처:
- llm.Router
- session.Store (workspace-scoped)
- usage.CallMeta (context propagation)
- serverauth.WorkspaceID
- 외부 callbacks (`ResolveProviderOverride`, `EstimateTokensCost`, `RunPrompt`, `SessionStoreForWorkspace`, `Now`)

→ **TARS 의 중심 노드**. ID-005 변경 시 import 50+ 파일 영향 — 인벤토리로 검증됨.

### gateway 결정 백로그 (3세션 정리)

| Finding | Status |
|---------|--------|
| RF-050 — semaphore 코드 중복 | open |
| RF-051 — CommandExecutor wiring 0 | open |
| RF-052 — consensus vote 미구현 | open |
| RF-053 — finalizeRunLocked 200줄 분리 | open |
| RF-054 — event 중복 publish | open |
| RF-055 — nodes 3 hardcoded dead | open |
| RF-056 — Telegram channelID fallback 명시화 | open |
| RF-057 — archive flag 의미 분리 | open |
| Q-014 — persistSnapshot retry 효용 | open |
| Q-015 — consensus 활성 빈도 | open |
| Q-016 — nodes 사용 빈도 | open |
| **ID-005** — gateway → agentruntime 패키지명 변경 | open |

→ **8 RF + 3 Q + 1 ID** = gateway 패키지 16건. 그 중 ID-005 (이름 변경) 가 가장 무거움.

### TARS 의 *"agent execution platform"* 정체성 명확화

s21-s23 종합 — 패키지 description (CLAUDE.md *"Agent execution platform"*) 가 정확. 5 책임 (lifecycle + executor + channels + consensus + persistence) 모두 *"실행 + 조정 + 영구화"* 성격. **"gateway" 명명만 mismatch** [ID-005].

→ ID-005 진행 시 description 과 명명 정렬.

## 다음 세션 진입점 (s24)

gateway 패키지 종료. 권장 순서 (s20 의 추천):
- **(D) `internal/llm`** — ✅ 완료 (s18-20)
- **(B) `internal/session`** — chat session store + transcripts. session_aggregator + memory_search/prompt fallback 모두 의존
- **(C) `internal/cron`** — Tick-based scheduler. cron 빌트인 툴 의존
- **(A) `internal/config`** — YAML + env override (대규모, 35+ gateway field 포함)
- **(E) `frontend/console/`** — Svelte 5 SPA. ID-001/002 결정의 사용자 표면

→ s24 권장: **`internal/session`** — gateway 의존성 + memory/prompt 의존성 + 전체 chat 핵심. 또는 **`internal/config`** — ID-005 Phase 3 (config 마이그레이션) 의 핵심 입력. config 가 큰 패키지라 사용자 결정.
