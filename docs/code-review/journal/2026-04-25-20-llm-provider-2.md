---
date: 2026-04-25
session: 20
scope: internal/llm/{openai_codex_client,gemini_native,gemini_native_chat,gemini_native_convert,model_lister}.go (~2100줄) — provider 2차 + model lister. **llm 패키지 종료**
next: backend 인프라 (gateway/session/cron) 또는 frontend (s21+)
findings: [RF-048, RF-049, ID-004 capability 매트릭스 완성]
---

# Session 20 — `internal/llm/` provider 2차 (OpenAI Codex 우선) + 패키지 종료

사용자 ID-004 우선순위 ("OpenAI Codex 부터") 적용. provider 2차 분석 + capability 매트릭스 완성.

## 다룬 파일

- `openai_codex_client.go` (858) ⭐ — 가장 큰 provider, ID-004 1순위
- `gemini_native.go` (91) — 진입
- `gemini_native_chat.go` (430) — Chat + preflight + thinking config
- `gemini_native_convert.go` (417) — wire format conversion
- `model_lister.go` (302) — 모델 discovery (4 provider)

총 ~2100줄. **llm 패키지 종료** (총 3 세션, 4475줄).

## 한눈에

### OpenAI Codex — 가장 큰 + 가장 빈약 ⚠️

`openai_codex_client.go` 858줄 — TARS 의 가장 큰 provider 파일. 그러나 **capability 가 가장 빈약** (4/13).

**좋은 패턴**:
- **OAuth 자동 갱신** — 401/403 + refresh_token 있으면 자동 retry
- **Stream-required fallback** — 일부 모델이 stream=true 강제 시 자동 streaming retry
- **Transient error retry** — `internal_error`/`stream id` 패턴 매칭 시 재시도
- **Tool name sanitization 양방향 매핑** — Responses API 의 `[a-zA-Z0-9_-]` 이름 제약 우회. `outboundByOriginal` + `originalByOutbound` 양방향 변환
- **AccountID JWT 자동 추출** — credential 에 없으면 JWT 에서 자동 파싱
- **Encrypted reasoning content** include — Codex 의 reasoning chain 보존

**capability gap (ID-004 1순위)**:
- `tool_choice: "auto"` **hardcoded** (line 413) — caller ChatOptions.ToolChoice 무시 [RF-048]
- ReasoningEffort/ThinkingBudget/ServiceTier 모두 silent 무시 [RF-049]
- Structured output 미지원 (Responses API 의 `text.format` 미활용)
- PDF placeholder (anthropic + gemini-native 와 격차)

→ **OpenAI Codex 가 anthropic 의 30% 수준**. 사용자 우선순위 *"OpenAI 1순위"* 정확함.

### Gemini Native — 의외로 anthropic 다음 ⭐

`gemini_native_*.go` 938줄. capability 9/13 — gemini-compat (4/13) 보다 훨씬 풍부.

**좋은 패턴**:
- **Preflight check** (`ensureModelSupportsGenerateContent`) — 첫 호출 시 모델이 generateContent 지원하는지 사전 검증. sync.Mutex + cached result. 모델 잘못 설정 시 chat 호출에서 명확한 에러
- **Thinking 양차원 지원** — `ThinkingLevel` (effort: MINIMAL/LOW/MEDIUM/HIGH) + `ThinkingBudget` (token count) 둘 다. anthropic 보다 더 풍부 (anthropic 은 budget 만)
- **Multimodal PDF native** ✅ — inlineData 형식, anthropic 과 동급
- **Function calling 표준 매핑** — geminiFuncDecl + ParametersJsonSchema
- **API version splitting** — base URL 에서 v1beta/v1 자동 추출

**미구현**:
- responseSchema (strict structured output) — geminiGenConfig 에 필드 없음
- Prompt caching 명시화 (자동 caching 사용 가능성 미문서화)

→ **gemini-native 가 ID-004 의 gemini 부분 우선순위 낮춤**. responseSchema 1개만 추가하면 anthropic 동급.

### Model Lister — 4 provider discovery

`model_lister.go` 302줄. `FetchModels(ctx, opts)` 가 provider 별:
- openai → `/models` 엔드포인트
- anthropic → 자체 모델 list 엔드포인트
- gemini/gemini-native → ListModels API
- openai-codex → `https://api.openai.com/v1/models`

→ runtime 에 사용자가 선택할 모델 list 제공. 콘솔/CLI 의 model picker 인풋.

## ID-004 capability 매트릭스 완성 (s19 + s20)

자세한 매트릭스는 [ID-004 본문](../findings/ideas.md#id-004) 참고. 핵심 점수:

| Provider | 점수 (13점 만점) | 비고 |
|----------|-------------------|------|
| anthropic | **12/13** | 표준 수준. effort 없음만 미흡 |
| gemini-native | **9/13** ⭐ | 의외 — PDF + Thinking 양차원, 단 responseSchema 미구현 |
| openai-compat | **7/13** | reasoning_effort + service_tier 만 |
| openai-codex | **4/13** ⚠️ | hardcoded tool_choice + advanced field 모두 silent ignore |
| gemini-compat | **4/13** ⚠️ | gemini-native 가 우월. deprecate 후보 |
| claude-code-cli | 0/13 (special) | CLI 자체 도구. capability 무관 |

→ **사용자 우선순위 정량 검증 완료**:
- OpenAI 1순위 ✓ — openai-codex 가 격차 가장 큼 (8 점 차)
- Gemini 도 같은 수준 목표 → gemini-native 는 거의 도달 (3점 차), gemini-compat 은 deprecate 후보

## 새 발견 (RF급)

### RF-048 — `openai_codex` 의 `tool_choice: "auto"` hardcoded

ID-004 Phase 1 핵심. caller ChatOptions.ToolChoice 무시. Responses API 의 `tool_choice: { type: "function", "name": "X" }` 활용.

### RF-049 — `openai_codex` 의 ChatOptions advanced fields silent 무시

ReasoningEffort/ThinkingBudget/ServiceTier/ResponseFormat (신규) 모두 silent 무시. Responses API 의 `reasoning: { effort }` + `text: { format }` + `service_tier` 활용. ID-004 Phase 1 의 OpenAI Codex 작업 본체.

## RF-042 (strict mode) 평가 — provider 2차 결과

s17 의 RF-042 (subagents_plan normalization 단순화):

| Provider | Strict tool calling | RF-042 적용 가능성 |
|----------|---------------------|---------------------|
| anthropic | ✅ specific tool name | **즉시 가능** |
| openai (compat) | ⚠️ ChatOptions 확장 필요 | medium |
| openai-codex | ❌ hardcoded → RF-048 의존 | RF-048 후 medium |
| gemini-native | ⚠️ Mode string only (specific tool 어려움?) | 부분 가능 |
| gemini (compat) | ⚠️ string only | 동일 |
| claude-code-cli | ❌ 무관 | 적용 불가 |

→ **anthropic 환경에서만 RF-042 즉시 가능**. 다른 provider 는 RF-048/049 + ChatOptions 확장 후.

## 새 관점 ("필요한가/유용한가") 평가

| Provider | 평가 |
|----------|------|
| openai-codex | ✅ 필수 (사용자 우선순위 1순위, OAuth-only ChatGPT backend) — 단 capability 격차 해소 필요 |
| gemini-native | ✅ 필수 (anthropic 다음 풍부) |
| gemini-compat | ⚠️ gemini-native 우월. **deprecate 후보** — ID-004 Phase 5 |
| claude-code-cli | ⚠️ 사용 빈도 의문 — Q-013 |

→ ID-004 Phase 5 의 *"gemini-compat deprecate 결정"* 정량 데이터 강해짐: gemini-native 가 9/13, gemini-compat 이 4/13. **gemini-compat 의 모든 사용자가 gemini-native 로 마이그레이션 가능**.

## CLAUDE.md 정렬

| 약속 | 실제 | 평가 |
|------|------|------|
| *"openai-codex": OAuth-only via ChatGPT backend* | openai_codex_client OAuth flow + 자동 refresh | ✅ |
| *"gemini-native": raw API (preflight)* | preflight + supportedActions 검증 | ✅ |
| *"Multimodal (text/image/document)"* | gemini-native ✅ + openai-codex ⚠️ + openai ⚠️ [RF-046] | ⚠️ |
| *"Tool calling"* | 모두 지원하지만 ToolChoice specific 은 anthropic 만 | ⚠️ |
| *"infrastructure that every session needs"* | 6 provider 모두 cfg-driven | ✅ |

## 핵심 인사이트 — llm 패키지 3세션 종합

### Provider capability hierarchy (정량)

```
[표준]   anthropic        12/13 — 모든 차원 지원
   ↓
[2위]    gemini-native     9/13 — Thinking 양차원 + PDF, responseSchema 미구현
   ↓
[3위]    openai-compat     7/13 — reasoning_effort + service_tier
   ↓
[빈약]   openai-codex      4/13 ⚠️ — hardcoded + silent ignore (ID-004 1순위)
[빈약]   gemini-compat     4/13 ⚠️ — gemini-native 가 우월 (deprecate 후보)
   ↓
[특수]   claude-code-cli   0/13 — CLI 자체 도구
```

→ ID-004 작업 우선순위 (사용자 우선순위 정렬):
1. **openai-codex** [RF-048 + RF-049] — 격차 가장 큼, 사용자 1순위
2. **openai-compat** — ChatOptions 확장 (ResponseFormat) + ToolChoice specific
3. **gemini-native** — responseSchema 추가 (1 항목만)
4. **gemini-compat deprecate** — gemini-native 가 우월
5. **anthropic** — 이미 표준 수준, 유지

### 보안 가드 7번째 = openai-codex OAuth 자동 갱신

s12-s19 의 6 가드 + s20 의 openai-codex OAuth flow:
1. RegistryScope (s12)
2. PathPolicy (s13)
3. web_fetch SSRF (s14)
4. subagent triple guard (s16)
5. flow validation (s17)
6. NewRouter 7-validation (s18)
7. **OpenAI Codex OAuth 자동 갱신 + transient retry** (s20)

→ 7가드 모두 *"silent skew 방지"* 또는 *"resilience"* 패턴.

### llm 패키지 architecture

```
[Layer 1] tier (heavy/standard/light)
    ↓
[Layer 2] role (8 semantic callers)
    ↓
[Layer 3] router (config-driven dispatch)
    ↓
[Layer 4] Client interface (Ask/Chat) + ChatOptions
    ↓
[Layer 5] 6 provider 구현 (anthropic/openai/openai-codex/gemini/gemini-native/claude-code-cli)
    ↓
[Layer 6] transport + http_utils + errors + redaction (공통)
```

→ **응집도 높은 패키지**. 새 provider 추가 시 같은 패턴 따라가면 됨. CLAUDE.md *"Single resolution path"* 정렬.

### 결정 백로그 종합 (s10-s20)

큰 결정 백로그:
- **RF-007** (빌트인 플러그인 제거) + **RF-008** (sh 훅 제거) + **RF-009** (외부 HTTP 정책)
- **ID-001** (KB 유지/단순화/확장 — 가설 A 확정)
- **ID-002** (시스템 프롬프트 헤더 외부화)
- **ID-003** (빌트인 툴 통합/분리 정책 — 4 통합 후보)
- **ID-004** (OpenAI/Gemini capability 격차 해소 — OpenAI Codex 1순위)
- **Q-001/002** (env loader / serve-api flag)
- **Q-011** (process 빈도) + **Q-012** (subagents plan/orchestrate 빈도) + **Q-013** (claude-code-cli 빈도)

→ 사용자 결정 11건 (TN 3 + ID 4 + Q 4) + RF 49건 정리 후 단계적 진행.

## 다음 세션 진입점 (s21+)

llm 패키지 종료. 다음 후보:

- **(A) `internal/gateway`** — 에이전트 실행 플랫폼. subagents/sessions/message 빌트인 툴이 의존. 큰 패키지 가능
- **(B) `internal/session`** — chat session store + transcripts. session_aggregator + memory_search/prompt fallback 모두 의존
- **(C) `internal/cron`** — Tick-based scheduler. cron 빌트인 툴이 의존
- **(D) `internal/config`** — YAML + env override. ResolveAllLLMTiers (s18 인풋)
- **(E) `frontend/console/`** — Svelte 5 SPA. ID-001/002 결정의 사용자 표면

권장: (A) gateway → (B) session → (D) config → (C) cron → (E) frontend. gateway 가 가장 많이 참조되고 큰 패키지일 가능성.

llm 종료 → 코드 리뷰 진척 ~95%. 5세션 정도 더면 종료 후 overview.md 작성.
