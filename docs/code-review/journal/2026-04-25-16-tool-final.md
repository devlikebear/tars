---
date: 2026-04-25
session: 16
scope: internal/tool/{tool_telegram,tool_gateway,tool_sessions,session_aggregator,automation,automation_context,automation_cron,tool_subagents}.go (~1700줄) — telegram/gateway/sessions/cron/subagents
next: internal/tool/{tool_subagents_plan,tool_subagents_orchestrate}.go (~1170줄, s17) — subagents plan/orchestrate 본체. tool 패키지 종료
findings: [RF-039, RF-040, ID-003 보강]
---

# Session 16 — `internal/tool/` telegram/gateway/sessions/cron/subagents

tool 패키지 마지막 단계. 큰 분량 (~2.8k줄 남음) 이라 분할:
- **s16 (이번)**: 짧은 빌트인 툴 + dispatch 파일들 (~1700줄)
- **s17 (다음)**: subagents_plan + subagents_orchestrate (~1170줄)

## 다룬 파일

- `tool_telegram.go` (111) — telegram_send 단일 툴
- `tool_gateway.go` (162) — message/nodes/gateway 3 툴 (모두 cfg-flag optional)
- `tool_sessions.go` (279) — 6 individual session tools
- `session_aggregator.go` (62) — session aggregator (7 actions)
- `automation.go` (98) — cron aggregator (7 actions)
- `automation_context.go` (78) — context.Context 키들 (currentSessionID/Kind/TelegramTarget)
- `automation_cron.go` (553) — cron CRUD + run + alias resolution + reminder 추론
- `tool_subagents.go` (344) — subagents_run (parallel/consensus 모드)

총 ~1700줄.

## 한눈에

### 좋은 패턴 ⭐

#### 1. subagent 다층 safety guard (tool_subagents.go:285-310)

```go
func validateSafeSubagent(info gateway.AgentInfo) string {
    // (1) safe agent 검증
    if info.Kind != "prompt" { return "must be a prompt-based agent" }
    if info.PolicyMode != "allowlist" { return "must use allowlist policy" }
    if len(info.ToolsAllow) == 0 { return "must define read-only tools_allow" }

    // (2) high-risk tool block
    for _, name := range info.ToolsAllow {
        if isHighRiskSubagentTool(name) { return "allows high-risk tool" }
    }
}

func isHighRiskSubagentTool(name string) bool {
    canonical := CanonicalToolName(name)
    switch canonical {
    case "exec", "process", "write_file", "edit_file", "apply_patch", "workspace":
        return true
    }
    return strings.HasPrefix(canonical, "write_") || strings.HasPrefix(canonical, "edit_")  // (3) catch-all
}
```

→ **Triple guard**: agent kind/policyMode/toolsAllow 검증 + tool blocklist + prefix 기반 catch-all. **read-only 보장 강력**. RegistryScope (s12) + PathPolicy (s13) + web_fetch SSRF (s14) 와 함께 **모범 보안 패턴 4개째**.

CLAUDE.md *"max 4 subagents"* 와 같은 카테고리 — gateway depth/threads cap 도 여기서 적용.

#### 2. cron 의 context-aware 스코프 해결 (automation_cron.go:260-302)

`resolveCronScopeFromContext` 가 **현재 chat session 의 kind 에 따라 cron 의 default scope 결정**:

```go
switch currentSessionKind {
case "main":
    // global cron (모든 세션 영향)
    sessionID = ""
    sessionTarget = "main"
default:
    // 일반 chat session 에서 만든 cron 은 그 세션에 바인딩
    sessionID = currentSessionID
    sessionTarget = ""
}
```

추가로 alias `current` / `global` / `isolated` / `none` 정상화 + `currentTelegramTarget` context 통해 응답 채널 자동 보존.

→ **silent default 없음**. 사용자가 *"이거 매일 알려줘"* 라고 하면 그 chat 세션에 자동 바인딩 — 직관적.

#### 3. session aggregator description 의 sub-action 안내

```
"Manage chat sessions. Actions: status (current session info), list (all sessions), history (read chat history), send (send message and wait), spawn (async agent run), runs (list/get/cancel async runs), agents (list available agents)."
```

각 action 의 의미를 짧게 명시 → LLM 이 어떤 action 쓸지 결정 도움. memory aggregator 의 "라우팅 hint" (s15) 와 같은 카테고리.

#### 4. cron 의 reminder 자동 추론

`inferCronTaskType` + `inferReminderMessage`:
- "알림"/"remind"/"notification" 키워드 → reminder task type
- 자연어 prompt → reminder message 자동 추출 (한국어 + 영어)

→ 자연스러운 LLM 사용 패턴 지원. *"매일 9시에 운동 알림 줘"* → 자동으로 reminder task + telegram 채널 기억.

## 새 발견 (RF급)

### RF-039 — `cron` aggregator 의 다중 alias silent acceptance

`name`/`title` + `prompt`/`message` + `id`/`job_id` + `task_type` legacy. 4 resolution 함수에 분산:
- `resolveCronJobName` / `resolveCronJobPrompt` / `inferCronTaskType` / `inferReminderMessage`

→ alias 의도 추적 어려움. schema 에 alias 명시 없어 silent acceptance. RF-029 (read_file) + RF-031 (exec) 와 같은 카테고리지만 표면 더 큼.

### RF-040 — `message` / `nodes` / `gateway` 빌트인 툴 통합 또는 외부화

3 빌트인 툴 모두 같은 gateway runtime + 같은 cfg-flag 군집 + 모두 admin 성격. 통합 후보 또는 외부화 후보.

옵션:
- **(a)** 단일 `gateway` aggregator 통합 (9 sub-actions, cfg-flag 1개)
- **(b)** 외부화 (skill+CLI) — admin 워크플로는 콘솔이 더 적합
- **(c)** 현 상태 유지 — 세밀한 cfg flag

→ ID-003 결정에 종속. 시스템 프롬프트 비용 ~150 토큰/turn (켜졌을 때 3 툴 합).

## 새 관점 ("필요한가/유용한가" + "통합/분리") 평가

| 툴 | 평가 | 통합 가능? |
|----|------|----------|
| `telegram_send` | ✅ 필수 (delivery 채널) | 단독 OK. 다른 IM 추가 시 통합 후보 |
| `message` | ⚠️ ID-003 외부화 후보 | gateway 와 통합 가능 |
| `nodes` | ⚠️ admin 성격 | 통합 또는 외부화 |
| `gateway` | ⚠️ admin status/reload/restart — 빈도 매우 낮음 | 통합 또는 외부화 |
| `session` | ✅ 이미 통합 (7 actions) | 통합됨. partial schema [ID-003 보강 필요] |
| `cron` | ✅ 이미 통합 (7 actions) | 통합됨. RF-039 alias 정리 + Q-011 빈도 평가 |
| `subagents_run` | ✅ 필수 (multi-agent 인프라) | s17 plan/orchestrate 와 통합 가능 |

→ **외부화 후보**: message + nodes + gateway (3개). **alias 정리 필요**: cron + session [partial schema].

## ID-003 보강 (s16)

### `message`/`nodes`/`gateway` 통합 = ID-003 의 또 다른 명확한 케이스

ID-003 결정 갈래:
- **(B) 통합 정책**: 3 툴 → 1 `gateway` aggregator (9 sub-actions). 시스템 프롬프트 -150 토큰/turn
- **(D) 부분 외부화**: 3 툴 모두 외부화 (admin 워크플로 콘솔 적합)

s14 의 web aggregator 와 함께 **(B)/(D) 결정의 두 가지 케이스**:
1. **web** (search + fetch, 2 actions) — 단순. 통합 또는 유지
2. **gateway-message-nodes** (9 actions) — 복잡. 통합 또는 외부화

추가 케이스:
3. **file** (read/write/edit/list/glob/apply_patch, 6 actions) — 중간. s13 분석.

→ 사용자 결정 시 web/gateway-message-nodes/file 3개를 같이 평가.

## CLAUDE.md 정렬

| 약속 | 실제 | 평가 |
|------|------|------|
| *"max 4 subagents"* + *"max depth"* | gateway_subagents_max_threads/max_depth, subagents_run 에 cap | ✅ |
| *"telegram delivery"* infrastructure | telegram_send 빌트인 | ✅ |
| *"infrastructure that every session needs"* | message/nodes/gateway 의문 [RF-040] | ⚠️ |
| *"Avoid backwards-compatibility hacks"* | cron alias 4쌍 [RF-039] | ⚠️ |
| *"Default pattern: skill+CLI"* | message/nodes/gateway 외부화 후보 [RF-040] | ⚠️ |

## 핵심 인사이트

### 모범 보안 패턴 정리 (코드 리뷰 누적)

지금까지 본 강력한 보안 가드 4개:
1. **`RegistryScope` panic 가드** (s12) — wiring time
2. **`PathPolicy` symlink escape** (s13) — read/write 분리
3. **`web_fetch` SSRF 다층 가드** (s14) — scheme/IP/DNS/redirect
4. **`subagent` triple guard** (s16) — agent kind + tool blocklist + prefix catch-all

→ **코드베이스 전체의 보안 의식 일관됨**. 다른 영역의 보안 코드 작성 시 이 4개가 reference 모델.

### subagent + cron + telegram 의 결합

cron 작업 → telegram 응답 채널 자동 보존 → subagent 가 백그라운드 실행 → 결과 telegram 전달. **자연스러운 자동화 워크플로**.

`automation_context.go` 의 context 키들 (`currentSessionID`/`Kind`/`TelegramTarget`) 이 이 workflow 의 backbone.

### s16 검토 결과 정리 (alias 표면)

코드베이스의 alias 표면 누적:
- read_file: offset/limit + start_line/end_line + max_bytes [RF-029]
- exec: command/cmd [RF-031]
- cron: name/title + prompt/message + id/job_id + task_type [RF-039]
- session aggregator: partial schema (sub-params 추측) [ID-003]
- memory/knowledge/workspace: 동일 [s15]

→ aggregator 패턴이 LLM ergonomic 위해 alias 풍부하게 받지만, **문서화 없이 분산** + schema 외부 silent acceptance. **codebase-wide alias 정책** 필요할 정도.

## 다음 세션 진입점 (s17, tool 마지막)

`internal/tool/` 의 마지막 두 큰 파일:

- `tool_subagents_plan.go` (629줄) — subagents_plan
- `tool_subagents_orchestrate.go` (539줄) — subagents_orchestrate

총 ~1170줄. subagent 워크플로 (plan → orchestrate → run) 의 plan + orchestrate 단계.

이 단계의 새 관점 적용:
- 각 단계의 **필요성** — plan/orchestrate가 정말 필요한 빌트인 인프라?
- **통합** — `subagents_run` 과 함께 단일 `subagents` aggregator 가능?
- **complexity vs 효용** — 1170줄의 무게가 정당한가?

s17 마치면 **tool 패키지 종료** → s18 부터 frontend / 기타 패키지 (cron/session/gateway 본체, llm router, cli).
