// Package mcp exposes a small public wrapper around TARS' MCP client. It lets
// lightweight agent applications list MCP tools, adapt them into tools.Tool
// values, and call them directly without depending on TARS config or server
// internals.
package mcp
